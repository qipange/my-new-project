// Utility functions for the project

function greet(name) {
    return `Hello, ${name}! Welcome to the project!`;
}

function add(a, b) {
    return a + b;
}

function multiply(a, b) {
    return a * b;
}

module.exports = {
    greet,
    add,
    multiply
};